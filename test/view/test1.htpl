h1#po-o.po_o @poo-id="poop" @poopy-poo=3 poopoo-poo=3.45 string="string \"with 'quotes' inside" fnutts='singlefnutts " string \' "adfasdf' &poo="Popasdfasdf" "moo"="muuu" $variable=(stuff() + poop()) $poo=("Poo") $variable=["array" "with" "stuff"]
    "Poop"
%if is_traversable($result)
#%if $result is traversable
    #$attr=[class="poo" @poo="oop"]
    header $attr
        h1
            #"Hello"
    ul#poo class="list" &menu-id="poo" @rand=(rand() % 10) @prefix=[a=2 b=3 c=3.5]
        %for $key $value in $result
            li @id=$value.id()
                %encode($value)
%else
    div.empty @poo="oop"
        #"Poo"
